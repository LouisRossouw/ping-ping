services:
  ping-app:
    build: .
    image: mr-ping-ping
    volumes:
      - ./data:/app/data
      - ./configs:/app/configs
      - ./lib:/app/lib
      - /app/venv
    restart: unless-stopped
    ports:
      - "5005:5005"